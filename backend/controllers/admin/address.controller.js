const base = require("../address.controller");

module.exports = {
  getAddresses: base.getAddresses,
  getAddress: base.getAddress,
  createAddress: base.createAddress,
  updateAddress: base.updateAddress,
  deleteAddress: base.deleteAddress,
};
